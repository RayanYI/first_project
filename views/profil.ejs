<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profil</title>
</head>
<body>
    <% if(data){ %>
    <% data.forEach((element) =>{ %>
        <p> <%= element %> </p>
    <% }); %>   
    <% } %>
</body>
</html>